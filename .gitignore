# 1. DENY EVERYTHING
# *

# 2. ALLOW DIRECTORY TRAVERSAL
# We must allow git to "look inside" directories, otherwise it can't 
# find the specific files we want to whitelist below.
!*/

# 3. GLOBAL WHITELIST (Top-level files)
# Un-ignore specific files in the home root
!.gitignore
!.bashrc
!.inputrc

# 4. FOLDER WHITELIST
# Un-ignore specific configuration folders entirely
!.config/nvim/
!.config/fish/
!.config/ghostty/config

# 5. EXPLICIT RE-IGNORE (Secrets)
# Because step 4 whitelisted the entire 'fish' folder, we must 
# explicitly re-ignore the secrets file inside it.
.config/fish/secrets.fish
.config/nvim/lazy-lock.json
